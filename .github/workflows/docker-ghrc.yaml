name: Build & Push to GHCR

on:
  push:
    branches:
      - dev
    tags:
      - 'v*'

jobs:
  build-and-push-ui:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker tags
        id: docker_tags
        run: |
          # Get build number
          BUILD_NUM=${{ github.run_number }}

          # Default values - for dev branch
          UI_TAGS="ghcr.io/${{ github.repository_owner }}/trac-portal-ui:dev-build.${BUILD_NUM} ghcr.io/${{ github.repository_owner }}/trac-portal-ui:dev"

          # If this is a tag push (for master)
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
            UI_TAGS="ghcr.io/${{ github.repository_owner }}/trac-portal-ui:${VERSION} ghcr.io/${{ github.repository_owner }}/trac-portal-ui:latest"
          fi

          echo "ui_tags=${UI_TAGS}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build UI Image
        run: |
          docker build --no-cache -t temp-ui-image .

      - name: Tag and Push UI Image
        run: |
          for TAG in ${{ steps.docker_tags.outputs.ui_tags }}; do
            docker tag temp-ui-image $TAG
            docker push $TAG
          done
