name: Build & Push to GHCR

on:
  push:
    branches:
      - master
    tags:
      - 'v*'
  pull_request:
    branches:
      - master

jobs:
  build-and-push-ui:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker tags
        id: docker_tags
        run: |
          BUILD_NUM=${{ github.run_number }}
          ORG_NAME=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')

          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}
            UI_TAGS="ghcr.io/${ORG_NAME}/${REPO_NAME}:${VERSION} ghcr.io/${ORG_NAME}/${REPO_NAME}:latest"
          elif [[ "${{ github.event_name }}" == "pull_request" ]]; then
            UI_TAGS=""
            VERSION=pr
          else
            VERSION=pre-release
            UI_TAGS="ghcr.io/${ORG_NAME}/${REPO_NAME}:master-build.${BUILD_NUM} ghcr.io/${ORG_NAME}/${REPO_NAME}:pre-release"
          fi

          echo "ui_tags=${UI_TAGS}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

      - name: Pull pre-release image
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          ORG_NAME=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
          docker pull ghcr.io/${ORG_NAME}/${REPO_NAME}:pre-release
          docker tag ghcr.io/${ORG_NAME}/${REPO_NAME}:pre-release temp-ui-image

      - name: Build UI Image
        if: (github.event_name == 'push' && github.ref == 'refs/heads/master') || github.event_name == 'pull_request'
        run: |
          docker build --no-cache --build-arg VERSION=${{ steps.docker_tags.outputs.version }} -t temp-ui-image .

      - name: Tag and Push UI Image
        if: github.event_name != 'pull_request' && steps.docker_tags.outputs.ui_tags != ''
        run: |
          for TAG in ${{ steps.docker_tags.outputs.ui_tags }}; do
            docker tag temp-ui-image $TAG
            docker push $TAG
          done
